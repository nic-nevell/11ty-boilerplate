---
layout: 'base.njk'
---

<section class="section | ptb-lg">
	<div class="wrapper">
		<article class="article | | space-x">
			<h1>{{ title }}</h1>
			<img src="{{ image }}" alt="{{ imageAlt }}">

			{{ content | safe }}
		</article>
	</div>
</section>